plugins {
    id 'com.android.application'
}

android {
    namespace 'io.github.hexstr.UnityFPSUnlocker'
    compileSdk 34

    defaultConfig {
        applicationId "io.github.hexstr.UnityFPSUnlocker"
        minSdk 28
        targetSdk 34
        versionCode 1
        versionName "1.0"

        externalNativeBuild {
            cmake {
                cppFlags "-std=c++17 -fno-rtti -fno-exceptions"
                abiFilters "arm64-v8a"
                arguments "-DANDROID_PLATFORM=android-28", "-DCMAKE_BUILD_TYPE=Release"
            }
        }
    }

    // ★修正1：階層を一つ上げて、一番大元の CMakeLists.txt を読み込ませる
    externalNativeBuild {
        cmake {
            path "../cpp_engine/CMakeLists.txt"
        }
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
    buildFeatures {
        buildConfig = true
        viewBinding = true
    }
}

dependencies {
    implementation 'org.jetbrains.kotlin:kotlin-stdlib-jdk8:2.3.0'
    implementation 'androidx.appcompat:appcompat:1.7.1'
    implementation 'androidx.constraintlayout:constraintlayout:2.2.1'
    implementation 'androidx.preference:preference:1.2.1'
    compileOnly("de.robv.android.xposed:api:82")
}
